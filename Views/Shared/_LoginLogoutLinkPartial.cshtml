@if (Context.User.Identity.IsAuthenticated)
{
    @* var user = await _userManager.GetUserAsync(Context.User); *@
    <a asp-controller="user" asp-action="perfil" class="nav-link text-black"><b>  @* @user.NomeCompleto.PrimeiraPalavra().ToUpper() *@</b></a>
    <a class="nav-link pe-0 navbar-dark botaoLog" asp-controller="User" asp-action="Logout">Sair</a>
}
else
{
    <a class="nav-link pe-0 navbar-dark botaoLog" asp-controller="User" asp-action="Login">Entrar</a>
}