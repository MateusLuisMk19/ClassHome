@model TurmaUserModel
@inject ClassHomedbContext _context
@{
    var conf = _context.Configs.FirstOrDefault(x => x.Estado == Estado.Ativado);

    ViewBag.Subtitulo = "Add Professor à Turma";


}

<h1 class="text-light">@ViewBag.Subtitulo</h1>
<hr>
<partial name="_MensagemPartial"></partial>
<form asp-action="AddProffPost" class="row">
    <input type="hidden" asp-for="@Model.TurmaId">
    <div class="form-group col-sm-8 col-lg-3">

        <label asp-for="@Model.UserId">Professor</label><br>
        <select asp-for="@Model.UserId" class="form-control" asp-items="@(IEnumerable<SelectListItem>)ViewBag.PRF">
            <option disabled selected>Escolha o Professor</option>
        </select>
        <span asp-validation-for="@Model.UserId" class="text-danger"></span>

    </div>
    <div class="mt-3">
        <a asp-action="Index" class="@conf.BTNoff">Cancelar</a>
        <button class="@conf.BTNin" type="submit">Salvar</button>
    </div>
</form>